<% if (locals.flash) { %>
  <div class="flash flash-<%= flash.type %>">
    <span class="flash-icon"><i class="ph <%= flash.type === 'error' ? 'ph-x' : 'ph-check' %>" aria-hidden="true"></i></span>
    <%= flash.message %>
  </div>
<% } %>
